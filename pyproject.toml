[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "political-clarity"
version = "0.1.0"
description = "Local QEvasion political clarity and evasion detection pipeline."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]

